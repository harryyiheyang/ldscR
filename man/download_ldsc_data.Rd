% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_ldsc_data.R
\name{download_ldsc_data}
\alias{download_ldsc_data}
\title{Download and Load Precomputed LDSC Reference Data}
\usage{
download_ldsc_data(
  file = c("eurldsc", "afrldsc", "easldsc", "snpinfo"),
  load_into_env = TRUE
)
}
\arguments{
\item{file}{A character string specifying which LDSC reference file to download.
Must be one of `"eurldsc"`, `"afrldsc"`, `"easldsc"` or `"snpinfo"`.}

\item{load_into_env}{Logical; if \code{TRUE} (default), the downloaded file will be loaded into \code{.GlobalEnv}.}
}
\value{
Invisibly returns the path to the downloaded `.rda` file.
}
\description{
This function downloads `.rda` files (e.g., EURLDSC, AFRLDSC, EASLDSC, SNPInfo)
from an external Netlify server and optionally loads the data into the global environment.
}
\examples{
\dontrun{
# Download and load EURLDSC into current session
download_ldsc_data("eurldsc")

# Just download SNPInfo without loading
path <- download_ldsc_data("snpinfo", load_into_env = FALSE)
}
}
